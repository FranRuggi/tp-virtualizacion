import json
import time

def generar_token(usuario):
    # simulación de autenticación
    payload = {"user": usuario, "exp": int(time.time()) + 3600}
    token = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.abc123DEF456ghi789JKL.lmnopQRStuvWXYZ987654321"
    print("Token generado:", token)
    return token

print("Inicio del programa")
usuario = "admin@example.com"
jwt = generar_token(usuario)
